{
  "name": "bookloop-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "install:all": "npm install",
    "dev": "concurrently \"npm run dev -w bookloop-backend\" \"npm run start -w bookshop\"",
    "start:all": "concurrently \"npm run start -w bookloop-backend\" \"npm run start -w bookshop\"",
    "test:all": "npm run test -w bookloop-backend && npm run test -w bookshop"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "prettier": "^3.2.5"
  }
}
